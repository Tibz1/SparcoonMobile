<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Sparcoon" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Créer composant texte par défaut-->
    <style name="DefaultTextStyle">
        <item name="android:textColor">@color/lightGray</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/sen</item>
    </style>

    <!-- Créer composant Bienvenue-->
    <style name="WelcomeTextStyle" parent="DefaultTextStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">24sp</item>
    </style>

    <!-- Créer composant Rubriques  sur écran d'accueil -->
    <style name="RubriqueHomeTextStyle" parent="WelcomeTextStyle">
        <item name="android:fontFamily">@font/senbold</item>
    </style>

    <!-- Créer composant Nom client accueil -->
    <style name="ClientNameHomeTextStyle" parent="DefaultTextStyle">
        <item name="android:fontFamily">@font/senbold</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>